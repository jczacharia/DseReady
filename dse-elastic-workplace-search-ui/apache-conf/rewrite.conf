LoadModule rewrite_module "/opt/rh/httpd24/root/usr/lib64/httpd/modules/mod_rewrite.so"

RewriteEngine On
RewriteRule "^index\.html$" "-" [L]

# Pass through real files and directories under DOCUMENT_ROOT
RewriteCond "%{DOCUMENT_ROOT}%{REQUEST_URI}" "-f" [OR]
RewriteCond "%{DOCUMENT_ROOT}%{REQUEST_URI}" "-d"
RewriteRule "^" "-" [L]

# Do NOT rewrite reverse-proxied paths to the SPA shell.
# /api/*       -> legacy dsesearchapi
# /adv/*       -> advanced search UI pod
# /adv/api/*   -> advanced search API pod
RewriteCond "%{REQUEST_URI}" "!^/api(/|$)"
RewriteCond "%{REQUEST_URI}" "!^/adv(/|$)"
RewriteRule "." "/index.html" [L]
